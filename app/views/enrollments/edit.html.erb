<% content_for :title do %>
  Corseg
  |
  Editing
  <%= @enrollment.to_s %>
<% end %>

<div class="container">
  <div class="card">
    <div class="card-header">
      <h3><i class="fa fa-lock-open"></i> Editing enrollment</h3>
    </div>
    <div class="card-body">
      <%= render 'form' %>
    </div>
    <div class="card-footer">
      <%= link_to 'Show', @enrollment %>
      <%= link_to 'Back', enrollments_path %>
    </div>
  </div>
</div>