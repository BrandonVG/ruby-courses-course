<div class="container">
    <%= link_to "Courses", courses_path %>
  /
  <%= link_to @course.title, course_path(@course) %>
  / new lesson
  <div class="card">
    <div class="card-header">
      <h3><i class=" far fa-check-square"></i> New lesson</h3>
    </div>
    <div class="card-body">
      <%= render 'form' %>
    </div>
    <div class="card-footer">
      <%= link_to 'Back to course', course_path(@course) %>
    </div>
  </div>
</div>