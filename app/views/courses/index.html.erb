
<div class="container">
  <h1>Courses: <%= @courses.size %></h1>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <% @courses.each do |course|%>
      <div class="col">
        <%= render 'courses/course', course: course  %>
      </div>
    <% end %>
  </div>
</div>